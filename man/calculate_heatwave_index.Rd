% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/calculate_heatwave_index.R
\name{calculate_heatwave_index}
\alias{calculate_heatwave_index}
\title{Calculer les indices de vagues de chaleur}
\usage{
calculate_heatwave_index(
  data,
  threshold = 35,
  min_duration = 3,
  period = "annual",
  season = "summer"
)
}
\arguments{
\item{data}{Dataframe climatique contenant au minimum les colonnes
\code{date}, \code{year}, \code{month} et \code{tmax}.}

\item{threshold}{Seuil de température (°C) définissant un jour chaud.
Par défaut : 35°C.}

\item{min_duration}{Nombre minimal de jours consécutifs pour définir une
vague de chaleur. Par défaut : 3 jours.}

\item{period}{Période de calcul : \code{"annual"} (défaut) ou
\code{"seasonal"}.}

\item{season}{Si \code{period = "seasonal"}, saison à analyser :
\code{"summer"} (mois 6-8), \code{"spring"} (mois 3-5),
\code{"autumn"} (mois 9-11), \code{"winter"} (mois 12,1,2).}
}
\value{
Un dataframe avec les colonnes :
\describe{
\item{year}{Année}
\item{n_hot_days}{Nombre de jours dépassant le seuil}
\item{n_heatwaves}{Nombre de vagues de chaleur}
\item{max_duration}{Durée maximale d'une vague (jours)}
\item{mean_tmax}{Température maximale moyenne (°C)}
}
}
\description{
Calcule plusieurs indices thermiques permettant d'identifier et quantifier
les vagues de chaleur : nombre de jours chauds, durée des vagues de chaleur
et température maximale moyenne par période.
}
\examples{
data(climate_morocco)
hw <- calculate_heatwave_index(climate_morocco, threshold = 38)
head(hw)

# Analyse saisonnière estivale
hw_summer <- calculate_heatwave_index(climate_morocco,
                                       threshold = 38,
                                       period = "seasonal",
                                       season = "summer")

}
